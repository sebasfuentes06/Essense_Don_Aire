{
  "name": "essence-don-aire",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Essence Don Aire: frontend (React + Vite) y API (Express + PostgreSQL) en un solo despliegue.",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "build": "npm --prefix frontend install && npm --prefix frontend run build",
    "dev": "npm --prefix frontend run dev",
    "dev:api": "npm --prefix backend run dev"
  },
  "dependencies": {
    "bcryptjs": "2.4.3",
    "cors": "2.8.5",
    "dotenv": "16.4.5",
    "express": "4.21.2",
    "jsonwebtoken": "9.0.2",
    "pg": "8.13.1"
  }
}
